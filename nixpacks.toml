[phases.setup]
nixPkgs = ["nodejs", "nodejs.pkgs.npm"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["echo 'No build step needed for backend'"]

[start]
cmd = "npm start"

[variables]
NIXPACKS_BUILD_DIR = "/backend"